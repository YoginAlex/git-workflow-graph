{
  "name": "git-graph",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@gitgraph/js": "^1.3.2",
    "parcel": "^1.12.3"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1"
  },
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url ./git-workflow-graph/",
    "deploy": "gh-pages -d dist"  
  }
}
